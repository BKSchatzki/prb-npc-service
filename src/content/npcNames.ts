import { aasimarNames } from './npc-names/aasimarNames';
import { dragonbornNames } from './npc-names/dragonbornNames';
import { dwarfNames } from './npc-names/dwarfNames';
import { elfNames } from './npc-names/elfNames';
import { gnomeNames } from './npc-names/gnomeNames';
import { goliathNames } from './npc-names/goliathNames';
import { halflingNames } from './npc-names/halflingNames';
import { humanNames } from './npc-names/humanNames';
import { orcNames } from './npc-names/orcNames';
import { tieflingNames } from './npc-names/tieflingNames';

const npcNames = {
  aasimar: aasimarNames,
  dragonborn: dragonbornNames,
  dwarf: dwarfNames,
  elf: elfNames,
  gnome: gnomeNames,
  goliath: goliathNames,
  halfling: halflingNames,
  human: humanNames,
  orc: orcNames,
  tiefling: tieflingNames,
};

export default npcNames;
